name: Test
description: Test

inputs:
  a:
    description: a
    required: false
  b:
    description: b
    required: false
    default: '${{ inputs.a }}'

runs:
  using: composite

  steps:
    - name: Test
      env:
        A: '${{ inputs.a }}'
        B: '${{ inputs.b }}'
      shell: bash
      run: |
        echo "a: $A"
        echo "b: $B"
