name: Test
description: Test

inputs:
  name:
    description: name
    default: ${{ inputs.dummy }}
  dummy:
    description: dummy
    default: 'dummy'

runs:
  using: 'composite'

  steps:
    - name: Test
      shell: bash
      run: |
        echo 'name: ${{ inputs.name }}'
        echo 'dummy: ${{ inputs.dummy }}'
