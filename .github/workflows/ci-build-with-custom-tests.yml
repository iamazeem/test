name: shell_test

on:
  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
    - name: unspecified
      run: echo 'unspecified'

    - name: sh -> dash
      shell: sh
      run: |
        ls -Gghl $(which sh)
        dpkg -s dash | grep 'Version'

    - name: bash
      shell: bash
      run: echo 'bash'
