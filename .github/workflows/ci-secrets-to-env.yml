name: secrets_to_env

on: workflow_dispatch

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
    - name: Set
      run: |
        echo '${{ toJSON(secrets) }}' | jq -r 'keys[] as $k | "\($k)=\"\(.[$k] | @base64)\""' >> "$GITHUB_ENV"

    - name: Get
      run: env
