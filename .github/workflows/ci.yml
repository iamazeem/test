name: ci

on:
  push:
    branches:
      - main
    ignore-paths:
      - '**.md'
  pull_request:
    branches:
      - main
    ignore-paths:
      - '**.md'

jobs:

  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run a one-line script
        run: |
          echo Testing!
