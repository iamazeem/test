name: zsv_on_macos_fuzz

on:
  push:
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  ci:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, macos-15-intel]
    runs-on: ${{ matrix.os }}

    steps:
    - name: Checkout
      uses: actions/checkout@v6
      with:
        repository: liquidaty/zsv
        ref: underflow-fix-v2

    - name: Install deps
      run: |
        brew update
        brew install coreutils afl++

    - name: Run fuzzing
      env:
        AFL_DEBUG: 1
      run: |
        sudo afl-system-config

        cd fuzz
        make afl-build

        echo "=== Running afl-showmap ==="
        afl-showmap -o /dev/null -- ./afl/fuzz < ../data/test/2tsv-2.csv

        echo "=== Running ./afl/fuzz with 2tsv-2.csv ==="
        ./afl/fuzz < ../data/test/2tsv-2.csv

        # echo "=== afl-run ==="
        # make afl-run

        # echo "=== --- --- --- ==="

        mkdir -p afl/optimized

        echo "=== Running afl-cmin.bash ==="
        afl-cmin.bash -e -i ../data/test -o ./afl/optimized -T "all" -- ./afl/fuzz || true

        echo "=== Running afl-cmin ==="
        afl-cmin -e -i ../data/test -o ./afl/optimized -T "all" -- ./afl/fuzz || true

        echo "=== Running afl-fuzz ==="
        mkdir -p afl/out
        afl-fuzz -u -i ../data/test/2tsv-2.csv -x csv.dict -o ./afl/out -- ./afl/fuzz
