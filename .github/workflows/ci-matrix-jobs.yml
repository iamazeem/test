name: matrix_jobs

on: workflow_dispatch

jobs:
  ci:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    steps:
    - name: Test
      run: |
        sleep 10
        if [[ $RUNNER_OS == 'Windows' ]]; then
          sleep 5
          exit 1
        fi
